@charset "UTF-8";
  /* 캐러셀 전체 높이 설정 (선택사항) */
  #imageCarousel {
    margin-bottom: 20px;
  }
  
  .carousel-inner{
	background-color: #ffebee;
	height: 300px;
  }
  
  /* 옅은 분홍색 배경 (#ffebee는 매우 옅은 분홍색) */
  .carousel-item {
    background-color: #ffebee;
    border-radius: 8px;
    height: 100%;
  }
  
  /* 이미지에 그림자 효과 추가 */
  .carousel-item img {
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  }
  
  /* 캡션 스타일링 */
  .carousel-caption.position-static {
    position: relative;
    left: auto;
    right: auto;
    bottom: auto;
    padding: 10px;
  }
  
  /* 컨트롤 버튼 색상 (기본은 흰색이라 어두운 색으로 변경) */
  .carousel-control-prev-icon, .carousel-control-next-icon {
    filter: invert(1) grayscale(100%);
  }
  
   /* 이미지 컨테이너 스타일 */
  .image-container {
    width: 500px;
    height: 250px; /* 원하는 높이로 조정 */
    overflow: hidden;
  }
  
  /* 이미지 스타일 */
  .image-container img {
    width: 100%;
    height: 100%;
    object-fit: contain; /* 이미지 비율 유지, 컨테이너 내부에 맞춤 */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  }

  .px-6 {
    padding-right: 11rem !important;
    padding-left: 11rem !important;
  }
  
  .gap-c1{
    gap: 9rem!important;
  }
  
  /* object-fit 속성 옵션:
     - contain: 이미지 비율을 유지하면서 컨테이너 안에 완전히 표시
     - cover: 이미지 비율을 유지하면서 컨테이너를 완전히 채움(이미지 일부가 잘릴 수 있음)
     - fill: 이미지 비율을 무시하고 컨테이너에 맞게 늘림
  */