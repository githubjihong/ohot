/**
 *  dm chat js
 */

/* const dmSocket = new SockJs("/ws/dm");
const stompClient = Stomp.over(dmSocket);

function connect(dmSubNo) {
    stompClient.connect({}, function (frame) {
        console.log("Connected: " + frame);

        //메세지 받는 경로
        stompClient.subscribe(`/chatRoomAll/${dmSubNo}`, dmMsg => {
            //메세지 단순출력 (<div>내용</div>에 넣기 등)
            //showMessage(dmMsg.body);

            //받은 메세지 객체로 파싱 msg.sender, msg.content 등 구조 분해해서 사용 가능
            const dmMsgContent = JSON.parse(dmMsg.body)
            console.log(dmMsgContent);
        });
    });
} */

